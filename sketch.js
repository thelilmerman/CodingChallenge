//the setup function is performed once the sketch loads

function setup() {

    createCanvas(600,600);
    background(255);
    

    smooth()
}

//the draw function performed in a loop, 30 frames per seocond.
function draw() {
 )
}

